class ContactController {
  // Toda regra de negocio, se ficar dentro 
  index() {
    // Listar todos os registros
  }

  show() {
    // Obter UM registro
  }

  store() {
    // Criar novo registro
  }

  upadate() {
    // Editar um registro
  }

  delete() {
    // Deletar um registro
  }
}

// Sigleton -> somente uma instancia
module.exports = new ContactController();
